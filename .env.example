# Copy to .env and fill in. Both values are safe to expose in browser code —
# every privileged operation is gated by row level security in Postgres.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
