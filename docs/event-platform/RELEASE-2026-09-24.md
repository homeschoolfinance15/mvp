# Event repair release — 24 September 2026

## Deployed and checked

The feedback privacy, original-question preservation, event-update previews,
optional email subject, feedback rescheduling and concurrent check-in lock fixes
were committed as `d519ade` and deployed to production. Migration history was
aligned in `54142c9`; database deployment workflow
[36073158313](https://github.com/homeschoolfinance15/mvp/actions/runs/36073158313)
completed successfully. The event-email and event-mailer functions were also
deployed explicitly. No real attendee emails or charges were used for testing.

The live site https://goamazing.ai returned HTTP 200, as did its frontend entry
asset. At 23:43 UTC the bundle included the current event-update preview flow
and excluded the submitted-feedback readback button. This is a read-only
deployment smoke check, not a full production acceptance run.

Another session published changes concurrently. Its additive Stripe Vault
migration `20260925100501` was applied alongside the event repair and recorded
in source control. Later Stripe configuration changes are a separate release;
the event acceptance evidence does not certify all of those changes.

## Follow-up fixes deployed

- **R1 / ORG-05:** newly named cohosts default to no management access. The
  Hosting team section provides a separate management checkbox. An assigned
  manager can operate that event, including refunds and communications; review
  reading stays administrator-only. Existing cohosts retain their prior access
  during migration. Administrators/event managers can revoke it explicitly.
  Cohost emails link to the event and no longer promise management access.
- **R2 / ORG-14, ORG-15:** administrator editing no longer depends on the original
  creator retaining a hosting role.

These follow-up changes were deployed as **`2aac733`**. Migration
`20260925100601` was applied first from an isolated release snapshot.
Both [database deployment](https://github.com/homeschoolfinance15/mvp/actions/runs/36074847456)
and [frontend checks](https://github.com/homeschoolfinance15/mvp/actions/runs/36074847470)
completed successfully. At 23:53 UTC the live site and bundle returned HTTP 200;
the bundle included the explicit host-management controls, retained the update
preview and excluded feedback readback. Permission changes use the existing
event-host audit trigger. Remaining acceptance work is deferred until tomorrow.

Local validation: the expanded remaining-area integration suite passed all 26
checks, including grant/revoke, self-grant denial, administrator replacement,
email retry, 100 competing registrations and 50 concurrent distinct-ticket
scans. Frontend build, mailer type checking, mailer self-checks and 20 organizer
rules passed. Evidence: [remaining-results.json](uat-evidence/remaining-results.json).
The expanded browser/API suite also passed **48/48**, including the visible
management checkbox, saving a grant and revoking it, along with feedback,
signup, tickets, email previews and interrupted scanning regressions. Evidence:
[independent-results.json](uat-evidence/independent-results.json). Earlier browser
attempts required correcting the connector fixture and waiting for asynchronous
permission persistence; the completed run passed. Scoped lint reported no errors
and existing React warnings; the build retains its bundle-size warning.
The retained `uat-evidence/test-failure.png` captures an earlier test-harness
failure, not the result of the final passing run.

## Acceptance still needed

Real Stripe test-mode checkout, Connect and webhook/refund behavior; receipt
of emails in an authorized test inbox; actual deployed timer execution; and
physical-phone camera QR scanning remain unverified. Local simulated payments,
captured email requests and browser fallback tests do not establish these.
See the [follow-up audit](UAT-FOLLOWUP-2026-09-24.md) for coverage and limits.
